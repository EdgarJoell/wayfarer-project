import { ATagsDirective } from './a-tags.directive';

describe('ATagsDirective', () => {
  it('should create an instance', () => {
    const directive = new ATagsDirective();
    expect(directive).toBeTruthy();
  });
});
